{
  "name": "ts-telegram-bot",
  "version": "1.0.0",
  "description": "",
  "main": "main.ts",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "start:dev": "npx nodemon --watch 'src/**/*.ts' --exec 'ts-node' src/main.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf ./build && tsc",
    "start": "npm run build && node build/main.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.4.4",
    "@types/node-telegram-bot-api": "^0.61.6",
    "nodemon": "^3.0.1",
    "rimraf": "^5.0.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "node-telegram-bot-api": "^0.61.0"
  }
}